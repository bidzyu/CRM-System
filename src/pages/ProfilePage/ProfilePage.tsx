import { Flex } from 'antd';

const ProfilePage = () => {
  return (
    <Flex align={'center'} justify={'center'} style={{ height: '100vh' }}>
      <h1>Привет</h1>
    </Flex>
  );
};

export default ProfilePage;
